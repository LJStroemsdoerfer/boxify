# import libs
import subprocess
import os
import sys

# create main class
class container():
    """
    main class that turns scripts into containers
    container() class allows the users to create and containerize Flask APIs while
    working in their favorite Python IDE. The container() class builds on top of
    the Flask framework.
    Parameters
    ----------
    Examples
    --------
    >>> from contain import container
    >>> 
    """